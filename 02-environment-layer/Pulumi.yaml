name: 02-environment-layer
description: Environment lifecycle layer of infrastructure project
runtime:
  name: python
  options:
    toolchain: uv
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
